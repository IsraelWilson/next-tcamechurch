export default function Tally(props) {
  return (
    <div>
      <div>
        <label>{props.name}</label>
        <label>{props.count}</label>
      </div>
      <button></button>
      <style jsx>{`

      `}</style>
    </div>
  )
}
